services:
  app:
    build: .docker/app
    stdin_open: true
    tty: true
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: true
      CHOKIDAR_INTERVAL: 100
    ports:
      - "5174:5173"
    volumes:
      - ./app:/app
    working_dir: /app
    user: 1000:1000